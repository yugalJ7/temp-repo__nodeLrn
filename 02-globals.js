// Global variables in Node.js are variables that are defined outside of any function and can be accessed from anywhere in the application. They are properties of the global object, which is the root object of the Node.js runtime environment.

// __filename -> tell the filename -> /Users/ginger_xo/Visual Studio/node-learning/app.js
// __dirname -> path to current directory -> /Users/ginger_xo/Visual Studio/node-learning
// modules -> info about current module (file)
// process -> provides various information sets about the runtime of a program
// require -> function to use module

setInterval(() => {
  console.log("hala madrid");
}, 1000);
